# Rust TCP server
A TCP socket server, listening for connections.

## Build

Install Rust:

```bash
curl https://sh.rustup.rs -sSf | sh
```

Build project:

```bash
cargo build 
```

## Run

```bash
cargo run
```
